let hours = ['6am', '7am', '8am','9am','10am','11am','12pm','1pm','2pm','3pm','4pm','5pm','6pm', '7pm'];

let Seattle = {
  location: 'Seattle',
  min: 23,
  max: 65;
  avg: 6.3;
  
  function simulatedSales(min,max){
    let sales: null;

    for(let i = 0; i < hours.length; i++){
      sales[0] = 
    }

    return Math.floor(Math.random() * (max - min + 1) + min);
  }
}

let Toyko = {
  location: 'Toyko',
  min: 3,
  max: 24;
  avg: 1.2;
}

let Dubai = {
  location: 'Dubai',
  min: 11,
  max: 38;
  avg: 3.7;
}
let Paris = {
  location: 'Paris',
  min: 20,
  max: 38;
  avg: 2.3;
}

let Lima = {
  location: 'Lima',
  min: 2,
  max: 16;
  avg: 4.6;
}


Seattle.simulatedSales();